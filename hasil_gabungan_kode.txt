=== HASIL GABUNGAN KODE PROYEK ===
Direktori Sumber: C:\Users\user\Documents\Firstdeyan\portfolio-alya
======================================================================


================================================================================
/// FILE: index.html
================================================================================

<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Alya Rizqita - Eksekutif Administratif & Operasional</title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <script src="https://unpkg.com/@phosphor-icons/web"></script>
    
    <link rel="stylesheet" href="style.css">
</head>
<body>

    <header id="navbar">
        <div class="container nav-container">
            <div class="logo">Alya Rizqita.</div>
            <a href="#" class="btn-primary">Unduh Dokumen Lamaran</a>
        </div>
    </header>

    <section id="hero" class="container reveal">
        <div class="hero-content">
            <h1>Menghadirkan Keteraturan dalam Setiap Layanan Administratif.</h1>
            <p>Rekam jejak terukur dalam manajemen data, operasional layanan karir, dan penyelesaian masalah publik selama 2+ tahun di ITB Career Center.</p>
        </div>
        <div class="hero-image">
            <img src="assets/profile-alya.png" alt="Alya Rizqita">
        </div>
    </section>

    <section id="metrics" class="bg-light">
        <div class="container reveal">
            <div class="section-header">
                <h2>Service Level Agreement (SLA) & Pencapaian</h2>
            </div>
            <div class="metrics-grid">
                <div class="metric-card">
                    <i class="ph ph-hourglass-high"></i>
                    <h3>&lt; 24 Jam</h3>
                    <p>Resolusi respons komunikasi publik (Email, WA, Telepon).</p>
                </div>
                <div class="metric-card">
                    <i class="ph ph-users-three"></i>
                    <h3>200+</h3>
                    <p>Aktivasi keanggotaan baru berhasil diproses per bulan.</p>
                </div>
                <div class="metric-card">
                    <i class="ph ph-calendar-check"></i>
                    <h3>50+ Sesi</h3>
                    <p>Koordinasi konseling per bulan dengan tingkat kehadiran 95%.</p>
                </div>
            </div>
        </div>
    </section>

    <section id="reporting" class="container reveal">
        <div class="section-header">
            <h2>Manajemen Data & Visualisasi Laporan</h2>
            <p>Mengelola ribuan entri data pencari kerja dan menyajikannya dalam grafik analitik terstruktur untuk mendukung keputusan manajemen.</p>
        </div>
        <div class="browser-mockup">
            <div class="browser-header">
                <span class="dot dot-red"></span>
                <span class="dot dot-yellow"></span>
                <span class="dot dot-green"></span>
            </div>
            <img src="assets/dummy-dashboard.png" alt="Sistem Pelaporan">
        </div>
    </section>

    <section id="workflow" class="bg-light">
        <div class="container reveal">
            <div class="section-header">
                <h2>Arsitektur Alur Kerja Operasional</h2>
                <p>Standard Operating Procedure (SOP) pengelolaan layanan konseling yang sistematis dan efisien.</p>
            </div>
            <div class="timeline">
                <div class="timeline-item">
                    <div class="timeline-dot">1</div>
                    <div class="timeline-content">
                        <h4>Penerimaan & Validasi Data</h4>
                        <p>Verifikasi status mahasiswa/alumni yang mendaftar layanan melalui sistem.</p>
                    </div>
                </div>
                <div class="timeline-item">
                    <div class="timeline-dot">2</div>
                    <div class="timeline-content">
                        <h4>Alokasi Psikolog & Penjadwalan</h4>
                        <p>Pemetaan slot waktu kosong dan pencocokan jadwal antara peserta dan psikolog.</p>
                    </div>
                </div>
                <div class="timeline-item">
                    <div class="timeline-dot">3</div>
                    <div class="timeline-content">
                        <h4>Konfirmasi & Pelaksanaan</h4>
                        <p>Pengiriman notifikasi via email/WA untuk memastikan tingkat kehadiran maksimal.</p>
                    </div>
                </div>
                <div class="timeline-item">
                    <div class="timeline-dot">4</div>
                    <div class="timeline-content">
                        <h4>Rekapitulasi Pelaporan</h4>
                        <p>Penyusunan laporan volume sesi dan feedback peserta untuk pimpinan.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <footer>
        <div class="container">
            <h3>Siap memberikan standar pelayanan dan administrasi terbaik</h3>
            <p>alyarizqita22@gmail.com | +62 895 3754 72169</p>
            <p class="copyright">&copy; 2026 Alya Rizqita. All Rights Reserved.</p>
        </div>
    </footer>

    <script src="script.js"></script>
</body>
</html>

================================================================================
/// FILE: script.js
================================================================================

document.addEventListener('DOMContentLoaded', () => {
    
    // 1. Navbar Scroll Effect
    const navbar = document.getElementById('navbar');
    
    window.addEventListener('scroll', () => {
        if (window.scrollY > 20) {
            navbar.style.boxShadow = '0 4px 6px -1px rgba(0, 0, 0, 0.1)';
        } else {
            navbar.style.boxShadow = 'none';
        }
    });

    // 2. Scroll Reveal Animation (Intersection Observer)
    const reveals = document.querySelectorAll('.reveal');

    const revealOptions = {
        threshold: 0.15, // Elemen muncul saat 15% terlihat di layar
        rootMargin: "0px 0px -50px 0px"
    };

    const revealOnScroll = new IntersectionObserver(function(
        entries,
        observer
    ) {
        entries.forEach(entry => {
            if (!entry.isIntersecting) {
                return;
            } else {
                entry.target.classList.add('active');
                observer.unobserve(entry.target); // Hanya animasi satu kali
            }
        });
    }, revealOptions);

    reveals.forEach(reveal => {
        revealOnScroll.observe(reveal);
    });

    // Memicu animasi langsung untuk elemen yang sudah ada di layar saat pertama dimuat
    setTimeout(() => {
        reveals.forEach(reveal => {
            const rect = reveal.getBoundingClientRect();
            if (rect.top < window.innerHeight) {
                reveal.classList.add('active');
            }
        });
    }, 100);
});

================================================================================
/// FILE: style.css
================================================================================

/* Custom Properties & Reset */
:root {
    --primary-color: #004b87; /* Biru representasi korporat/ITB */
    --text-main: #0F172A;
    --text-muted: #475569;
    --bg-white: #FFFFFF;
    --bg-light: #F8FAFC;
    --border-color: #E2E8F0;
    --shadow-sm: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
    --shadow-lg: 0 10px 25px -3px rgba(0, 0, 0, 0.1);
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Inter', sans-serif;
    color: var(--text-main);
    background-color: var(--bg-white);
    line-height: 1.6;
}

.container {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 24px;
}

.bg-light {
    background-color: var(--bg-light);
}

.section-header {
    text-align: center;
    margin-bottom: 48px;
}

.section-header h2 {
    font-size: 2rem;
    font-weight: 700;
    margin-bottom: 12px;
}

.section-header p {
    color: var(--text-muted);
    max-width: 600px;
    margin: 0 auto;
}

section {
    padding: 96px 0;
}

/* Navigasi */
#navbar {
    position: fixed;
    top: 0;
    width: 100%;
    background-color: rgba(255, 255, 255, 0.9);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid var(--border-color);
    z-index: 1000;
    transition: all 0.3s ease;
}

.nav-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    height: 72px;
}

.logo {
    font-weight: 700;
    font-size: 1.25rem;
    letter-spacing: -0.5px;
}

.btn-primary {
    background-color: var(--primary-color);
    color: white;
    padding: 10px 20px;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 500;
    font-size: 0.9rem;
    transition: background-color 0.2s, transform 0.2s;
}

.btn-primary:hover {
    background-color: #003366;
    transform: translateY(-2px);
}

/* Hero Section */
#hero {
    display: flex;
    align-items: center;
    min-height: 100vh;
    padding-top: 72px; /* kompensasi navbar */
    gap: 48px;
}

.hero-content {
    flex: 1;
}

.hero-content h1 {
    font-size: 3rem;
    font-weight: 800;
    line-height: 1.2;
    letter-spacing: -1px;
    margin-bottom: 24px;
}

.hero-content p {
    font-size: 1.125rem;
    color: var(--text-muted);
    margin-bottom: 32px;
}

.hero-image {
    flex: 1;
    display: flex;
    justify-content: flex-end;
}

.hero-image img {
    width: 100%;
    max-width: 400px;
    aspect-ratio: 1/1;
    object-fit: cover;
    border-radius: 24px;
    box-shadow: var(--shadow-lg);
}

/* Metrics Dashboard */
.metrics-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 24px;
}

.metric-card {
    background: var(--bg-white);
    padding: 32px;
    border-radius: 16px;
    box-shadow: var(--shadow-sm);
    border: 1px solid var(--border-color);
    text-align: center;
    transition: transform 0.3s;
}

.metric-card:hover {
    transform: translateY(-5px);
    box-shadow: var(--shadow-lg);
}

.metric-card i {
    font-size: 3rem;
    color: var(--primary-color);
    margin-bottom: 16px;
}

.metric-card h3 {
    font-size: 2.5rem;
    font-weight: 800;
    letter-spacing: -1px;
    margin-bottom: 8px;
}

.metric-card p {
    color: var(--text-muted);
    font-size: 0.95rem;
}

/* Reporting System */
.browser-mockup {
    background-color: var(--bg-white);
    border-radius: 12px;
    overflow: hidden;
    box-shadow: var(--shadow-lg);
    border: 1px solid var(--border-color);
    margin: 0 auto;
    max-width: 900px;
}

.browser-header {
    background-color: #F1F5F9;
    padding: 12px 16px;
    display: flex;
    gap: 8px;
    border-bottom: 1px solid var(--border-color);
}

.dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
}
.dot-red { background-color: #FF5F56; }
.dot-yellow { background-color: #FFBD2E; }
.dot-green { background-color: #27C93F; }

.browser-mockup img {
    width: 100%;
    display: block;
}

/* Workflow Timeline */
.timeline {
    max-width: 800px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: 32px;
    position: relative;
}

.timeline::before {
    content: '';
    position: absolute;
    top: 0;
    bottom: 0;
    left: 24px;
    width: 2px;
    background-color: var(--border-color);
}

.timeline-item {
    display: flex;
    gap: 24px;
    align-items: flex-start;
    position: relative;
    z-index: 1;
}

.timeline-dot {
    width: 50px;
    height: 50px;
    background-color: var(--bg-white);
    border: 2px solid var(--primary-color);
    color: var(--primary-color);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    font-size: 1.25rem;
    flex-shrink: 0;
    box-shadow: 0 0 0 8px var(--bg-light);
}

.timeline-content {
    background-color: var(--bg-white);
    padding: 24px;
    border-radius: 12px;
    box-shadow: var(--shadow-sm);
    border: 1px solid var(--border-color);
    flex: 1;
}

.timeline-content h4 {
    font-size: 1.125rem;
    margin-bottom: 8px;
}

.timeline-content p {
    color: var(--text-muted);
    font-size: 0.95rem;
}

/* Footer */
footer {
    background-color: #0F172A;
    color: white;
    text-align: center;
    padding: 64px 0 32px;
}

footer h3 {
    font-size: 1.5rem;
    font-weight: 600;
    margin-bottom: 16px;
}

footer p {
    color: #94A3B8;
    margin-bottom: 32px;
}

footer .copyright {
    margin-top: 48px;
    padding-top: 24px;
    border-top: 1px solid #1E293B;
    font-size: 0.875rem;
    color: #64748B;
}

/* Responsive Design */
@media (max-width: 768px) {
    #hero {
        flex-direction: column-reverse;
        text-align: center;
        padding-top: 100px;
    }
    
    .hero-image img {
        max-width: 250px;
    }

    .hero-content h1 {
        font-size: 2.25rem;
    }
    
    .timeline::before {
        left: 20px;
    }
    
    .timeline-dot {
        width: 40px;
        height: 40px;
        font-size: 1rem;
    }
}

/* Animasi Muncul (Reveal) */
.reveal {
    opacity: 0;
    transform: translateY(30px);
    transition: all 0.8s ease-out;
}

.reveal.active {
    opacity: 1;
    transform: translateY(0);
}
